# Project Info
- **Purpose**: Standalone CLI Package (extracted from StabilityMatrix / ComfyUI ecosystem) for dependency extraction/analysis.
- **Languages / Tech Stack**: Python 3.12+, packaged with uv & pyproject.toml (Setuptools).
- **Structure**:
  - `dep_extractor/` - Main package (`core`, `analysis`, `install`, `output`, `cli.py`)
  - `tests/` - pytest test suite
- **Environment**: Operates under strict Agentic Global Rules (Beads issue tracking, Serena MCP architecture, Context7 documentation-first).