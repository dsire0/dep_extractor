# Suggested Commands
## Dev & Testing
- Dev Install: `uv pip install -e .` (Add `--system` if in miniconda base). Require `--help` flag check before running novel framework initializations.
- Run CLI: `dep-extractor --root <path>`
- Run Tests: `.\run_tests.ps1` (runs pytest)

## Work & Git
- Find Work & Manage Issues: `bd ready`, `bd update`, `bd close` (Beads tracking mandatory)
- Step-By-Step Commits Workflow: `/step-by-step-commits` (Must execute after any file edits)
- End Session: `git pull --rebase && bd dolt push && git push`

## System Navigation & Skills
- Use specific MCP tools over bash operations when available (e.g., `grep_search`, `list_dir`).
- Use Context7 MCP for exact library usage before writing new dependencies.
- Actively check workspace skills before attempting complex operations.